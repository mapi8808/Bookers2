<% if flash[:update] %>
      <div class="flash">
        <%= flash[:update] %>
      </div>
 <% end %>
 
 <% if flash[:signin] %>
      <div class="flash">
        <%= flash[:signin] %>
      </div>
 <% end %>
 
 <% if flash[:signup] %>
      <div class="flash">
        <%= flash[:signup] %>
      </div>
 <% end %>

<%= render 'users/list', user: @user, book: @book_new %>
<h2>Books</h2>
<table>
 <thead>
        <tr>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
        </tr>
    </thead>
　<tbody>
　<% @books.each do |book| %>
　<tr>
　　<div class="profile-container">
    <td><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, format: 'png', fallback: "no_image.png" %></td>
 <td>
    <%= link_to book.title, book_path(book.id) %>
    <!--構文合ってる？-->
 </td>
 </te>
  <div class="profile-opinion">
   <td><%= book.body %></td>
   </div>
   </div>
  <% end %>
 </tbody>
</table>
<!--下のはいらない？-->
<!--@books（モデルBook)なので、-->