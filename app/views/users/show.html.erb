<%= render 'users/list', users: @users %>
<h2>Books</h2>
<table>
 <thead>
        <tr>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
        </tr>
    </thead>
　<tbody>
　<% @books.each do |book| %>
　　<div class="profile-container">
    <%= attachment_image_tag @user, :profile_image, :fill, 20, 20, format: 'png', fallback: "no_image.png" %>
 <h4>Title</h4>
 <span>
    <%= link_to book.title, book_path(book.id) %>
    <!--構文合ってる？-->
 </span>
 <h4>Opinion</h4>
  <h3 class="profile-opinion"><%= book.body %></h3>
   
   </div>
  <% end %>
 </tbody>
</table>
<!--下のはいらない？-->
<!--@books（モデルBook)なので、-->