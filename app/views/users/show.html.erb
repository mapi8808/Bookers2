<%= render 'users/list', users: @users %>
<h2>Books</h2>
<table>
 <thead>
        <tr>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
        </tr>
    </thead>
　<tbody>
　<% @books.each do |book| %>
　<tr>
　　<div class="profile-container">
    <td><%= attachment_image_tag @user, :profile_image, :fill, 20, 20, format: 'png', fallback: "no_image.png" %></td>
 <td>
    <%= link_to book.title, book_path(book.id) %>
    <!--構文合ってる？-->
 </td>
  <div class="profile-opinion">
   <td><%= book.body %></td>
   </div>
   </div>
  <% end %>
 </tbody>
</table>
<!--下のはいらない？-->
<!--@books（モデルBook)なので、-->