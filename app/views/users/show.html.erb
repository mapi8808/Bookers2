<h2>Books</h2>
<table>
 <thead>
        <tr>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
        </tr>
    </thead>
　<tbody>
　<% @books.each do |book| %>
　　<div class="profile-container">
    <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
 <h4>Title</h4>
 <span>
    <%= link_to book.title, book_path(book.id) %>
    <!--構文合ってる？-->
 </span>
 <h4>Opinion</h4>
  <h3 class="profile-Opinion"><%= book.bo %></h3>
   
   </div>
  <% end %>
 </tbody>
</table>
<!--下のはいらない？-->
<!--@books（モデルBook)なので、-->
<%= render 'users/list', users: @users %>
